# 兔兔馆长拆件包

将本目录中的文件上传至：

`public/assets/library/rabbit/`

其中 `rabbit-rig.initial.json` 是第一版粗略坐标。打开仓库中的
`rabbit-rig.html` 后，可以拖动、缩放、旋转各图层并导出最终配置。

说明：
- 图片已转换为透明 WebP。
- 原始棋盘格背景已尽量去除。
- 首次打开仍需要人工微调位置，这是正常的“配准”步骤。
